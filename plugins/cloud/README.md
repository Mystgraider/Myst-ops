# cloud plugins

Extension point for Bogart Security Intelligence OS cloud capabilities.

Plugins should emit or consume the normalized contracts in
`bogart/security_intelligence_os/` instead of inventing per-tool schemas.
